# HKDF-SHA-512 for KDF key exchange.
# Overrides the HKDF-SHA-256 Kconfig default.
# BT_ACS_KDF_SALT_MAX_SIZE auto-expands to 64.
# Compose with overlay-kex-kdf.conf to restrict to KDF-only.
CONFIG_BT_ACS_KDF_HKDF_SHA512=y
CONFIG_PSA_WANT_ALG_SHA_512=y
