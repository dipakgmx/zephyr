# Curve25519 (X25519 / Montgomery) for ECDH key exchange.
# Overrides the P-256 Kconfig default; ECDH is already on from prj.conf.
# BT_ACS_ECDH_COORD_SIZE auto-expands to 32; BT_ACS_ECDH_HAS_Y=0 (x-only wire format).
CONFIG_BT_ACS_ECDH_CURVE_CURVE25519=y
CONFIG_PSA_WANT_ECC_MONTGOMERY_255=y
