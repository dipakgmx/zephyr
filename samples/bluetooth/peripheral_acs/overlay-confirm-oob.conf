# Advertise every ECDH confirmation capability (Tables 4.51 and 4.52) so the
# AC Client can select any method/action pair. The base build offers only
# Output Numeric.
CONFIG_BT_ACS_CONFIRMATION_OUTPUT_BEEP=y
CONFIG_BT_ACS_CONFIRMATION_INPUT_NUMERIC=y
CONFIG_BT_ACS_CONFIRMATION_INPUT_PUSH=y
