# ECDH-only: restrict key exchange to ECDH (P-256).
# Disables KDF and OOB from the full-blown base (prj.conf).
# PTS tests: BV-31-C, BI-07/08/13-C
CONFIG_BT_ACS_KEY_EXCHANGE_KDF=n
CONFIG_BT_ACS_KEY_EXCHANGE_OOB=n
