# P-521 curve (secp521r1) for ECDH key exchange.
# Overrides the P-256 Kconfig default; ECDH is already on from prj.conf.
# BT_ACS_ECDH_COORD_SIZE auto-expands to 66; BT_ACS_ECDH_HAS_Y stays 1.
CONFIG_BT_ACS_ECDH_CURVE_P521=y
CONFIG_PSA_WANT_ECC_SECP_R1_521=y
