# OOB-only: restrict key exchange to Out-of-Band.
# Disables ECDH and KDF from the full-blown base (prj.conf).
# OOB PSK must be loaded before PTS initiates BV-30-C.
CONFIG_BT_ACS_KEY_EXCHANGE_ECDH=n
CONFIG_BT_ACS_KEY_EXCHANGE_KDF=n
