# KDF-only: restrict key exchange to KDF (HKDF-SHA-256).
# Disables ECDH and OOB from the full-blown base (prj.conf).
# PSK must be provisioned out-of-band before PTS initiates BV-32-C.
CONFIG_BT_ACS_KEY_EXCHANGE_ECDH=n
CONFIG_BT_ACS_KEY_EXCHANGE_OOB=n
